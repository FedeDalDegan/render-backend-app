<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
    <link rel="stylesheet" href="../../public/css/home.css">
</head>
<body>
    <div id="usuarios">
        {{#if mostrarUsuarios}}
            <table>
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Email</th>
                        <th>Rol</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each users}}
                        <tr>
                            <td>{{this.first_name}}</td>
                            <td>{{this.email}}</td>
                            <td>{{this.rol}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        {{else}}
            <p>No se pueden mostrar los usuarios</p>
        {{/if}}
    </div>
</body>
</html>